apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "guacamole-vnc.fullname" . }}-test-connection"
  labels:
    {{- include "guacamole-vnc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "guacamole-vnc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
