apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "filerun.fullname" . }}-test-connection"
  labels:
    {{- include "filerun.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "filerun.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
